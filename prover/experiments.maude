

mod EXPERIMENT is 
	including INT . 

	sort AExp .

	---subsort Int < AExp .

	--- the plus sign cannot be used 
	op _+_ : AExp AExp -> AExp .

endm

mod EXPERIMENT1 is 
	including INT . 

	sort AExp .

	sort AllocationList .

	subsort AExp < AllocationList .
	subsort Int < AExp .

	op _,_ : AllocationList AExp -> AllocationList . 

endm